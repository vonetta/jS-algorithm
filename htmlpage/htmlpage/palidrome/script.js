var array = [];

function palindrome(str) {
    for (var i = str.length -1; i >= 0; i--) {
        console.log(str[i ]);
        array.push(str[i])
    }
        console.log(array)
}

palindrome("max");


